/**
 * 1. Loop through string finding substr
 */

function findSubstring(s: string, words: string[]): number[] {
  const tuple: number[] = [];

  return tuple;
}

const main = () => {
  console.log('🚀 Starting coding practice...');
  console.log('================================');

  console.log(findSubstring('barfoothefoobarman', ['foo', 'bar'])); // 5

  console.log('================================');
};

// Execute the main function
main();
